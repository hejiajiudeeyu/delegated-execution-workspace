# 回滚手册

> 英文版：[./rollback-playbook.md](./rollback-playbook.md)
> 说明：中文文档为准。

## 概览

回滚通过将 submodule SHAs 恢复到上一个已验证变更包完成，而不是先改写正式仓库历史。

## 前置条件

- 可访问 `changes/` 目录中的已验证变更包历史
- 可执行第四仓校验命令

## 步骤

1. 在 `changes/` 中定位最近一次已验证变更包。
2. 将子模块引用回退到该变更包记录的 SHAs。
3. 重新执行第四仓检查。
4. 确认组合通过全部校验。
